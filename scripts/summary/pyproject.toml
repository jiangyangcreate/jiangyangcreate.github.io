[project]
name = "blog-summary"
version = "1.0.0"
description = "本地生成博客 AI 摘要与播客音频，输出到 static/blog/summary/"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.0",
    "feedparser>=6.0",
    "parsel>=1.9",
    "websockets>=14.0",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
